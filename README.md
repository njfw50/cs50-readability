# Readability

This project implements a program that calculates the approximate grade level needed to comprehend some text, using the Coleman-Liau index.

## Usage
```bash
gcc readability.c -o readability -lm
./readability
```
